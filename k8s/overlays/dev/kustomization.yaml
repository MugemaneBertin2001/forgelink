apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: forgelink-dev

resources:
  - ../../base

nameSuffix: -dev

commonLabels:
  environment: development

# Development-specific patches
patches: []
